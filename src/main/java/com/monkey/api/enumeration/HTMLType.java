

package com.monkey.api.enumeration;
/**
 * Enumerate all the HTML types allowed by monkey.
 * When it lucks profil type we should add it here
 */
public enum HTMLType {

	img, button, input, textArea, radiobox, select, file, label, span, div, td, table, tr, a, li, ul, ol, obeject, section, fieldSet, form, iframe
}
